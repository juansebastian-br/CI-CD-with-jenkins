# Capturas requeridas

Añade aqui las evidencias:

- `github-actions-ci.png`: workflow de GitHub Actions en ejecucion o finalizado.
- `jenkins-cd.png`: Stage View o ejecucion del pipeline Jenkins.

Las capturas reales deben tomarse una vez los pipelines se ejecuten en tus plataformas.
